# AI夏令营音频生成竞赛 - 简化依赖
# 基础依赖
pandas>=1.5.0
numpy>=1.21.0
pyyaml>=6.0
python-dotenv>=0.19.0
tqdm>=4.64.0
rich>=12.0.0

# 音频处理 (基础)
librosa>=0.10.0
soundfile>=0.12.0

# 系统监控
psutil>=5.9.0

# 测试框架 (可选)
# pytest>=7.0.0
# pytest-cov>=4.0.0

# 可选依赖 (如果安装失败可以跳过)
# pyopenjtalk>=0.4.1  # 注释掉，避免安装问题
# torch>=1.13.0       # 如果需要GPU加速
# torchaudio>=0.13.0  # 如果需要GPU加速
